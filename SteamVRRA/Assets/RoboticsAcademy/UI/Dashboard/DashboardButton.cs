using System;
//using Microsoft.MixedReality.Toolkit;
//using Microsoft.MixedReality.Toolkit.Input;
//using Microsoft.MixedReality.Toolkit.Utilities;
//using Microsoft.MixedReality.Toolkit.WindowsMixedReality.Input;
using UnityEngine;

public class DashboardButton : MonoBehaviour
{
    void Update()
    {
        //foreach (IMixedRealityController controller in CoreServices.InputSystem?.DetectedControllers)
        //{
        //    if (controller.Visualizer?.GameObjectProxy != null && controller.ControllerHandedness == Handedness.Left)
        //    {
                
        //            gameObject.transform.position = pose.Position;
        //            gameObject.transform.rotation = Quaternion.LookRotation(-pose.Forward, -pose.Up);

        //            gameObject.transform.position = controller.Visualizer.GameObjectProxy.transform.position;
        //            gameObject.transform.rotation = controller.Visualizer.GameObjectProxy.transform.rotation;

                
        //    }
        //}
    }
}
